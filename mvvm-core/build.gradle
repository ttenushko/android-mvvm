plugins {
    id 'java-library'
    id 'org.jetbrains.kotlin.jvm'
}

archivesBaseName = "mvvm-core"
version = "$MVVM_CORE_VERSION_NAME"

java {
    sourceCompatibility = JavaVersion.VERSION_1_7
    targetCompatibility = JavaVersion.VERSION_1_7
}

kotlin {
    explicitApi()
}